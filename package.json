{
  "name": "my-project",
  "version": "1.0.0",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "start-backend": "python -m backend",
    "start-frontend": "cd frontend && npm run dev",
    "start-electron": "wait-on tcp:5173 tcp:8000 && electron ."
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "electron": "^15.5.7"
  },
  "dependencies": {
    "wait-on": "^8.0.4"
  }
}
